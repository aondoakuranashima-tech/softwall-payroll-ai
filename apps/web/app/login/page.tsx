'use client';

import { FormEvent, useState } from 'react';
import Link from 'next/link';
import { useRouter } from 'next/navigation';
const API_URL = process.env.NEXT_PUBLIC_API_URL || 'https://softwall-payroll-api-v2.onrender.com';
export default function LoginPage(){const router=useRouter();const[email,setEmail]=useState('');const[password,setPassword]=useState('');const[loading,setLoading]=useState(false);const[error,setError]=useState('');async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();setError('');setLoading(true);try{const res=await fetch(`${API_URL}/api/auth/login`,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({email,password})});const data=await res.json().catch(()=>({}));if(!res.ok)throw new Error(Array.isArray(data.message)?data.message.join(', '):(data.message||'Login failed.'));const token=data.access_token||data.token;if(token)localStorage.setItem('softwall_token',token);router.push('/')}catch(err){setError(err instanceof Error?err.message:'Login failed.')}finally{setLoading(false)}}const input={display:'block',width:'100%',boxSizing:'border-box' as const,marginTop:7,padding:12,border:'1px solid #d1d5db',borderRadius:8,fontSize:16};return <main style={{minHeight:'100vh',display:'grid',placeItems:'center',background:'#f7f8fa',fontFamily:'Inter,system-ui,sans-serif',padding:24}}><form onSubmit={submit} style={{width:'100%',maxWidth:440,background:'#fff',padding:32,borderRadius:16,border:'1px solid #e5e7eb'}}><h1 style={{marginTop:0}}>Log in</h1>{error&&<div role="alert" style={{padding:12,margin:'16px 0',background:'#fee2e2',color:'#991b1b',borderRadius:8}}>{error}</div>}<label>Email<input required type="email" value={email} onChange={e=>setEmail(e.target.value)} style={input}/></label><label style={{display:'block',marginTop:16}}>Password<input required type="password" value={password} onChange={e=>setPassword(e.target.value)} style={input}/></label><button disabled={loading} type="submit" style={{width:'100%',marginTop:22,padding:14,border:0,borderRadius:9,background:'#111827',color:'#fff',fontWeight:700}}>{loading?'Signing in…':'Log in'}</button><p style={{textAlign:'center'}}>Don't have an account? <Link href="/signup">Sign up</Link></p><p style={{textAlign:'center'}}><Link href="/">Back to home</Link></p></form></main>}
