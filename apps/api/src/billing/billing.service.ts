import {Injectable} from '@nestjs/common'; const PLANS:any={starter:{base:29,perUser:3},business:{base:99,perUser:5},enterprise:{base:499,perUser:8}}; @Injectable() export class BillingService{quote(plan:string,users:number){const p=PLANS[plan]??PLANS.starter;const seats=Math.max(1,Math.floor(users));return {plan,seats,monthly:p.base+p.perUser*seats,baseMonthly:p.base,perUserMonthly:p.perUser,currency:'USD',providers:['stripe','paystack']}}}
