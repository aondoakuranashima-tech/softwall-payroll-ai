# Billing
Use Stripe/Paystack behind a common provider interface. Webhooks are authoritative and must be signature-verified and idempotent. Pricing = platform fee + active billable seats × per-user fee.
